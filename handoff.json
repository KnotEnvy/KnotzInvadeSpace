{
  "$schema": "https://json.schemastore.org/package.json-ish-handoff",
  "handoffVersion": "2.1",
  "generatedOn": "2026-06-04",
  "lastUpdated": "2026-06-10",
  "audience": "The game is content-complete, optimized, SHIPPED, and (phase 11) has a fully scripted STORY CAMPAIGN — 'Operation Homecoming'. No required work remains. If you're touching the campaign, start at src/campaign.js (all sectors/waves/dialogue are authored data there) and 'keySystems.campaign' below. forTheOptimizationTeam / forTheVisualsTeam are retained as historical context for their layers.",
  "project": {
    "name": "Knotz: Invade Space",
    "tagline": "A modern, juiced-up roguelite reimagining of Space Invaders.",
    "genre": "Arcade shoot-'em-up with roguelite meta-progression",
    "repo": "https://github.com/KnotEnvy/KnotzInvadeSpace",
    "playUrl": "https://knotenvy.github.io/KnotzInvadeSpace/",
    "defaultBranch": "main",
    "license": "MIT",
    "playLoop": "Pick a mode (Campaign / Endless / Daily) -> play a run -> earn credits -> spend them in the Hangar on permanent upgrades -> chase the story Campaign victory, a higher score, deeper sectors, the daily leaderboard, and 14 achievements."
  },

  "status": {
    "summary": "SHIPPED & LIVE at https://knotenvy.github.io/KnotzInvadeSpace/ (GitHub Pages, served from main/root). Content-complete roguelite with the AAA visual pass (phase 9), the performance pass (phase 10), and the STORY CAMPAIGN overhaul (phase 11 — 'Operation Homecoming': 5 authored sectors, briefings, comms, warp jumps, 2 new bosses incl. a final Mothership). NOTE: phase 11 is committed on main locally — pushing to origin/main is what deploys it to the live site. No known runtime errors (validated headlessly across all states + modes + graphics tiers, incl. a full scripted campaign playthrough to victory).",
    "phasesCompleted": [
      { "phase": 1, "date": "2026-06-01", "title": "Modern arcade rebuild", "delivered": "Modular vanilla-JS engine: juice (shake/particles/hit-flash/trails/combos), Galaga dive AI, energy beam, 6 power-ups, multi-phase boss every 5th wave, endless background progression, synthesized Web Audio SFX+music, keyboard+touch, responsive canvas, localStorage hi-score, menu/pause/game-over." },
      { "phase": 2, "date": "2026-06-02", "title": "Roguelite meta + content", "delivered": "Persistent profile + Hangar shop (7 upgrades), new enemies (Stinger, Splitter) + elite modifier, Combat Drone, Magnet, twin Auto-Cannons, Aegis shield, splitting asteroid hazards, credit payouts." },
      { "phase": 3, "date": "2026-06-04", "title": "Boss & mini-boss variety", "delivered": "Rewrote boss.js into a DATA-DRIVEN class. BOSS_TYPES (Overlord/Weaver/Warden) rotate per encounter with distinct attack patterns (aimed/spread/ring/spiral/shotgun/curtain) + sweep styles; MINIBOSS_TYPES (Sentinel/Lancer) escort some waves with a floating health bar." },
      { "phase": 4, "date": "2026-06-04", "title": "Settings + accessibility + difficulty", "delivered": "New 'settings' state (settings.js): master/music/SFX volume sliders, screen-shake toggle, reduced-motion toggle, difficulty (easy/normal/hard), touch-controls mode. Save schema v2 with forward-migration. Difficulty fans out across enemy aggression/speed, boss HP/fire, credits and start lives." },
      { "phase": 5, "date": "2026-06-04", "title": "Achievements + Daily Challenge", "delivered": "achievements.js (14 unlockables, toasts, viewer) + daily.js (date-seeded run with 2 random modifiers; deterministic formations; per-day best). Mulberry32 PRNG in utils.js." },
      { "phase": 6, "date": "2026-06-04", "title": "Game modes + Campaign victory", "delivered": "CONFIG.modes (campaign/endless/daily). Campaign ends in a VICTORY screen after winBosses bosses (Earth finale background). Menu rebuilt with clickable mode chips + LAUNCH + Hangar/Settings/Awards buttons." },
      { "phase": 7, "date": "2026-06-04", "title": "Mobile on-screen controls", "delivered": "Rewrote input.js for multi-touch; on-screen ◀ ▶ / FIRE / BEAM buttons (UI.drawTouchControls). 'touchControls' setting: auto/on/off." },
      { "phase": 8, "date": "2026-06-04", "title": "Economy & difficulty balance pass", "delivered": "Credit payout nudged up (perScore .015, perBoss 75); added CONFIG.wave.maxSpeed cap for fair deep waves. Remaining balance is data in CONFIG for playtesters." },
      { "phase": 13, "date": "2026-06-11", "title": "Share-ready: SEO + social capsule art", "delivered": "index.html head: SEO title/meta description/canonical/theme-color/robots, full Open Graph + Twitter summary_large_image tags (absolute image URLs — required), PNG favicon (assets/icon-64.png) + apple-touch-icon (assets/icon-180.png), and JSON-LD VideoGame structured data (free, browser, single-player). Body gained a .sr-only crawlable/screen-reader summary (styles.css .sr-only) since the game is pure canvas. CAPSULE ART generated WITH THE GAME'S OWN ART: .smoke/card.html (gitignored compositor — Earth backdrop, boss + escorts + orb fire + the hero ship, title/tagline/feature chips/PLAY FREE/URL, 1200x630) captured by .smoke/card.js (headless-Edge CDP clip shots; NOTE captureBeyondViewport:true is required — off-viewport pixels come back black) into assets/og-card.png + the two icons; og-card.png doubles as the README hero. Regenerate any time with `node .smoke/card.js`. REMINDER: link previews only work once main is PUSHED (the og:image URL must be live); X caches cards — validate via twitter card validator / a fresh tweet draft." },
      { "phase": 12, "date": "2026-06-11", "title": "Campaign pass 2 — pacing, the Orion dock, writing & presentation", "delivered": "PACING: breathers between waves (CONFIG.wave.breather for endless/daily; CONFIG.campaign.waveDelay) and a longer pre-boss beat (campaign.bossDelay) during which the HIVEMIND taunt plays over the emptied arena; the post-boss loot window grew (campaign.warpWindow) and its last campaign.warpRamp ms visibly spool the starfield into warp; warp arrival fires Game.screenFlash (skipped under reduced-motion). THE DOCK: new 'dock' state — after each sector the run lands on the carrier UES ORION (new story cast member). enterDock() banks the sector's credit payout mid-run (Game.bankCampaignCredits, never double-pays vs endGame), runs Player.refit() (re-derives upgrade stats so dock purchases apply IMMEDIATELY, tops lives back up to startLives, refills energy, re-arms Aegis), and opens the Hangar in docked guise (header, banked-credits readout, DEPART button, the deck-chief dock line, your ship under repair with welding sparks + scanline; Esc departs, no quit-to-menu mid-run). departDock() refits again + rebuilds drones, then briefs the next sector. WRITING: full dialogue tighten; ORION woven through briefs/clears/docks; epilogue now 5 lines. PRESENTATION: Rajdhani (2nd Google Font, non-blocking) for all dialogue; Utils.text gained letterSpacing; briefing got an animated sector-progress map (cleared/current/future nodes + Earth marble); banners draw a cinematic gradient band + letter-spaced titles. Verified: smoke (incl. dock flow + full playthrough), node --check, headless screenshots." },
      { "phase": 11, "date": "2026-06-10", "title": "Story campaign overhaul — OPERATION HOMECOMING", "delivered": "NEW src/campaign.js: the campaign is now fully AUTHORED DATA, not endless-with-a-cap. 5 named sectors (Gateway Reach / The Shattered Belt / The Hive Nebula / The Blockade / Earth's Doorstep) each define backdrop, hazard+aggro profile, 3-5 scripted waves (exact cols/rows/speed + a composition `mix` that overrides the Wave formulas), scripted mini-boss escorts, a named sector boss, briefing dialogue, comm lines and a HIVEMIND taunt. CAST: VEGA (your stolen Hive ship's AI), EARTH COMMAND, and the HIVEMIND. NEW 'briefing' state: sector title card + typewriter story dialogue + threat readout, shown over a hyperspace-warp starfield (Starfield.setWarp; streaks gated by reduced-motion); FIRE fast-forwards then launches. In-mission comm cards (Game.say -> UI.drawComms, typewriter, speaker-coloured). Sector flow: boss death -> sector bonus (CONFIG.campaign.sectorBonus x sector) -> 3.4s loot/warp window -> jump (battlefield cleared) -> next briefing. NEW BOSSES (boss.js): VOID HERALD joins BOSS_TYPES (endless rotation is now 4 archetypes); FINAL_BOSS 'THE MOTHERSHIP' (campaign-only, scale 1.45, 4 phases, def.final drives victory). Campaign HUD: sector name + 'SECTOR n/5' + wave pips (dots per wave, diamond for the boss). Victory screen rolls a 4-line epilogue; campaign game-over shows 'FELL IN SECTOR n'. Persistence (save v3): campaign {best, wins} shown on the menu's campaign chip. Endless/Daily are untouched (campaign branches early in startWaveOrBoss/updateHazards/aggroMul)." },
      { "phase": 9, "date": "2026-06-05", "title": "AAA visual & juice pass", "delivered": "NEW src/postfx.js: offscreen threshold-bloom post-processing (drawImage+ctx.filter only, never getImageData; feature-detected with a hard no-op fallback). Graphics Quality setting (low/medium/high) in CONFIG.quality + Meta.quality()/fx()/extras()/trailsOn() scales bloom + particle budgets. JUICE: hit-stop (Game.freeze — a real-time sim-skip counter, fixed-timestep untouched), zoom-punch (Game.punchZoom), expanding Shockwave + textured SpriteBurst emitters (reusing the unused VFX columns 6-10 of boss.png), orb enemy bullets, brighter player bolts, beam halo+muzzle flare, combo escalation glow, muzzle recoil. ENTITIES/ENV: enemy warp-in + idle breathing + dive trails, player engine flames, power-up halos/trails, drone/asteroid trails+rim, richer starfield (shooting stars, hero-star flares), in-game vignette. SCREENS: HUD score-rollup + energy shimmer + low-life pulse, animated banner sweep-in, menu hover states, polished loading screen, boss muzzle telegraphs. SET-PIECES: campaign victory-over-Earth finale (horizon glow + rolling fireworks), boss-death cinematic, per-sector colour grade. All gated by Meta.reducedMotion()/shakeEnabled() for accessibility; Low tier ≈ pre-pass baseline." }
    ],
    "codeSize": { "srcFiles": 25, "srcLinesOfCode": 5267 }
  },

  "techStack": {
    "language": "Vanilla JavaScript (ES2020), no framework",
    "rendering": "HTML5 Canvas 2D",
    "audio": "Web Audio API (all SFX + music synthesized at runtime, zero audio files)",
    "build": "NONE. No bundler, transpiler, package.json, or node_modules.",
    "modules": "Plain classic <script> tags in dependency order (NOT ES modules) so the game runs by double-clicking index.html over file:// with no server. Each file defines globals; cross-file refs resolve at call time.",
    "runtimeForTooling": "Node v24.x (only for the optional headless smoke test / screenshots; not required to play)."
  },

  "howToRun": {
    "play": "Open index.html in any modern browser. No server or install.",
    "note": "Audio starts on first key press / tap (autoplay policy). Everything is synthesized.",
    "optionalLocalServer": "Only needed if you switch to ES modules or fetch(); then serve over http (npx serve / Live Server)."
  },

  "howToDevelop": {
    "workflow": "Edit files under src/, refresh the browser. No compile step.",
    "addingAFile": "Create src/yourfile.js and add a <script> to index.html at the right dependency position (see scriptLoadOrder).",
    "tuning": "Balance/look knobs live in src/config.js (CONFIG). Upgrades in src/meta.js (UPGRADES). Bosses in src/boss.js (BOSS_TYPES/MINIBOSS_TYPES). Achievements in src/achievements.js (ACHIEVEMENTS). Daily modifiers in CONFIG.daily.modifiers.",
    "debugging": "window.game exposes the live Game instance. window.game.openSettings('menu'), .openAchievements('menu'), .newGame('daily') etc. are handy.",
    "verification": "node --check src/*.js for syntax. For runtime, reuse the Node-VM smoke technique (see verification section) — it mocks the browser, concatenates all src except main.js, and drives every state/mode/boss."
  },

  "architecture": {
    "entryPoint": "src/main.js -> boot() on DOMContentLoaded: preloads assets, sizes canvas, runs the fixed-timestep loop.",
    "gameLoop": "Fixed-timestep sim. game.update(dt) always called with dt = CONFIG.STEP_MS (1000/60). Motion multiplies by k = dt/STEP_MS. Render decoupled per animation frame. Spiral-of-death guard caps catch-up at 5 steps.",
    "stateMachine": "game.state in: 'menu', 'hangar', 'settings', 'achievements', 'briefing' (campaign sector interstitial), 'dock' (mid-campaign refit on the UES Orion — the Hangar in docked guise), 'playing', 'paused', 'gameover'. Transitions centralized in Game.handleInput(); Game.draw()/update() branch on state. Overlay states ('settings','achievements') remember their opener via settingsReturn / achReturn.",
    "gameModes": "game.mode in 'campaign' | 'endless' | 'daily'. Set by newGame(mode) from the menu. Daily resolves game.dailyMods (a run-modifier aggregate) + game.dailyKey; helpers Game.aggroMul/playerDamageMul/bossEvery/waveRng read it. CAMPAIGN is scripted data (src/campaign.js): newGame('campaign') -> enterSector(1) -> 'briefing' -> launchSector() -> startCampaignWave() per beat; boss death -> warp window -> enterSector(n+1); the Mothership (def.final) ends in victory.",
    "entityManagement": "Pools for bullets (player+drone share game.bullets), enemyBullets, powerups, particles. Enemies in Wave.enemies; asteroids in game.asteroids; drones in game.drones; one boss/mini-boss at a time in game.boss.",
    "coordinateConventions": "Most entities use TOP-LEFT x/y + width/height (so Utils.aabb works). Gotchas: (1) Game.spawnBullet/spawnEnemyBullet take a CENTER and convert to top-left. (2) Asteroid stores a center but exposes a tightened x/y/w/h AABB. (3) Enemies store slotX/slotY; absolute pos = wave.x/y + slot. (4) Boss stores top-left x/y; width/height already include its scale.",
    "renderOrder": "Game.drawWorld(): starfield -> powerups -> enemy bullets -> asteroids -> wave enemies -> boss -> player bullets -> beam -> drones -> player -> particles. Then (outside the screen-shake translate): HUD + touch overlay + banner + pause/gameover overlays, and finally Ach.drawToasts() on top in EVERY state."
  },

  "scriptLoadOrder": [
    "src/config.js", "src/utils.js", "src/glowatlas.js", "src/audio.js", "src/input.js",
    "src/particles.js", "src/postfx.js", "src/starfield.js", "src/projectile.js", "src/powerup.js",
    "src/meta.js", "src/achievements.js", "src/daily.js", "src/player.js",
    "src/enemy.js", "src/boss.js", "src/campaign.js", "src/wave.js", "src/drone.js", "src/hazard.js",
    "src/ui.js", "src/hangar.js", "src/settings.js", "src/game.js", "src/main.js"
  ],

  "fileMap": {
    "src/config.js": { "exports": "CONFIG (global)", "role": "All tunables: resolution, timestep, player/bullet/enemy/wave/boss stats, hazards, drone, credits, palette (CONFIG.colors), background list, sprite-sheet frame layouts, AND newer blocks: settings defaults (incl. quality), difficulty presets, CONFIG.quality graphics tiers (bloom/particleMul/trails/fx/extras), modes, campaign, daily.modifiers, touch button layout, saveVersion, storageKey." },
    "src/postfx.js": { "exports": "PostFX (singleton)", "role": "Offscreen threshold-bloom post-processing. begin(ctx) returns the scene ctx (offscreen when bloom on, else the real ctx); end(ctx) bright-passes + blurs + composites additively, then UI draws crisp on top. drawImage + ctx.filter ONLY (never getImageData -> no file:// taint). Feature-detected once; no-ops if unsupported (covers old browsers + the Node-VM smoke harness). Quality params from Meta.quality()." },
    "src/glowatlas.js": { "exports": "GlowSprites (singleton)", "role": "PERF: pre-rendered glow sprite cache. blob(color)/orb(color) bake a radial glow ONCE into a small offscreen canvas (cached by colour), so hot draws blit additively (drawImage) instead of paying per-frame ctx.shadowBlur + createRadialGradient. Wired into Particle.draw, enemy-orb + friendly-bolt Bullet.draw. Feature-detected like postfx.js; returns null -> callers fall back to their original shadowBlur path (covers old browsers + the Node-VM smoke harness). Parses #rgb/#rrggbb; drawImage only (no getImageData -> no file:// taint)." },
    "src/utils.js": { "exports": "Utils", "role": "Math (clamp/lerp/rand/randInt/pick/chance/dist), Utils.aabb, easing, commas, Utils.text + Utils.roundRect canvas helpers, and Utils.makeRng (mulberry32) + Utils.hashStr (seed the Daily)." },
    "src/audio.js": { "exports": "AudioEngine, Sound (singleton)", "role": "Synthesized SFX + procedural music. setMuted + setVolumes({master,music,sfx}) layered over base bus levels via _applyVolumes(); safe to call before init()." },
    "src/input.js": { "exports": "Input", "role": "Unified keyboard + MULTI-touch + mouse. touches Map; high-level intents left/right/fire/beam (keys + on-screen buttons); buttonRect/_touchOn/buttonsActive for the touch overlay; wasPressed/consumePressed for menus. Maps client coords to the 720x960 design space." },
    "src/particles.js": { "exports": "Particle, Popup, Particles", "role": "Pooled particles (explosions/sparks/muzzle/thruster) + floating score popups. Reduced-motion thins counts via _count() and skips thruster; bullet trails handled in projectile.js." },
    "src/starfield.js": { "exports": "Starfield", "role": "Parallax background: the sector's space image (drifting/looping) + 3 scrolling star layers. setBackground(index). setWarp(0..1): hyperspace for campaign jumps — star speed boost + streak rendering, eased via lerp; calmer under reduced-motion." },
    "src/projectile.js": { "exports": "Bullet, Beam", "role": "Pooled glowing bullets (friendly flag distinguishes player vs enemy; reduced-motion skips trails). Beam = continuous energy column doing DPS." },
    "src/powerup.js": { "exports": "POWERUP_TYPES, PowerUp", "role": "Falling collectibles: rapid, spread, shield, energy, bomb, life." },
    "src/meta.js": { "exports": "UPGRADES, MetaProfile, Meta (singleton)", "role": "ROGUELITE CORE + persistence. Save schema v3 with forward-migration: { version, hi, credits, upgrades, muted, settings, achievements, daily, campaign:{best,wins} }. Upgrade catalog; derived run modifiers; settings getters (shakeEnabled/reducedMotion/difficulty/diffMods); achievements (isUnlocked/unlockAch); daily best (dailyBest/setDailyBest); campaign progress (campaignBest/setCampaignBest/campaignWins/addCampaignWin). award(score,waves,bosses,extraMul)." },
    "src/achievements.js": { "exports": "ACHIEVEMENTS, Achievements, Ach (singleton)", "role": "14 unlockables. Semantic hooks (onKill/onCombo/onWave/onBossKill/onScore/onCredits/onUpgrade/onVictory/onDailyComplete) decide unlocks; 'apex' rolls up after the 3 boss kills. drawToasts() (slide-in cards, any state) + drawScreen() (the 'achievements' viewer)." },
    "src/daily.js": { "exports": "DailyChallenge, Daily (singleton)", "role": "Date-seeded run. todayKey() identity; modifiersFor(key) picks CONFIG.daily.pickCount distinct modifiers deterministically; aggregate() merges patches onto neutral(); waveSeed(key,n) seeds per-wave composition; today() cached snapshot; best(key)." },
    "src/player.js": { "exports": "Player", "role": "Ship: movement+banking, primary fire (single/twin/spread/rapid), energy/beam, shield, lives, i-frames, applyPowerUp(). reset() reads Meta upgrades + difficulty + daily startLives; refit() re-derives those stats mid-run at the campaign dock (repairs lives up to startLives, refills energy, re-arms Aegis); shoot() applies game.playerDamageMul(); takeHit() clears game.bossHitless." },
    "src/enemy.js": { "exports": "Enemy, Beetlemorph, Rhinomorph, Stinger, Splitter", "role": "Formation/dive/return/dying states + Galaga dive AI. makeElite() promotes any enemy. Splitter bursts into diving minions via game.queueMinion()." },
    "src/boss.js": { "exports": "BOSS_TYPES, MINIBOSS_TYPES, FINAL_BOSS, Boss", "role": "DATA-DRIVEN bosses. Each def: name/frameY/color/move(sine|figure8|lumber)/scale/hpMul/scoreMul/fireBase/fireMin/phases. runStep() turns an attack-step {type:aimed|spread|ring|spiral|shotgun|curtain} into bullets. FOUR archetypes rotate in endless (Overlord/Weaver/Warden/Void Herald); FINAL_BOSS = THE MOTHERSHIP (campaign-only finale, 4 phases, def.final). Boss accepts opts.def so the campaign scripts which boss guards each sector. Mini-bosses (opts.mini) are smaller, drop loot, don't clear the sector. Difficulty scales HP + fire cadence." },
    "src/campaign.js": { "exports": "SPEAKERS, CAMPAIGN_SECTORS, CAMPAIGN_EPILOGUE, Campaign (singleton)", "role": "OPERATION HOMECOMING — all campaign content as authored data: 5 sectors (name/sub/bg/hazardMul/bigAsteroid/aggro/boss id/threat/dock line/brief dialogue/taunt/clearSay/waves[]). Cast: VEGA, ORION (the carrier UES Orion), COMMAND, HIVEMIND. Wave descriptors carry exact cols/rows/speed + a `mix` that overrides Wave's formulas + optional mini/say. Campaign.sector(n)/count()/bossFor(def)/speakerColor(who)/carrier. Edit THIS file to retune or extend the story." },
    "src/wave.js": { "exports": "Wave", "role": "Formation marcher. constructor(...,rng,mix): composition RNG is seeded for the Daily; `mix` (campaign) overrides the waveCount-formula chances with scripted ones. create() rolls enemy types/elites (+ daily eliteChanceAdd). Aggression = Meta.diffMods().enemyFireMul * game.aggroMul(). Detects invasion (loss)." },
    "src/drone.js": { "exports": "Drone", "role": "Auto-firing wingman from the Combat Drone upgrade; fires from the shared bullet pool." },
    "src/hazard.js": { "exports": "Asteroid", "role": "Rotating asteroid that falls, shatters big->med->small. Daily can raise spawn rate + big chance." },
    "src/ui.js": { "exports": "UI", "role": "ALL on-screen UI: HUD (drawHUD — campaign shows sector name + wave pips instead of WAVE/LEVEL), the menu (drawMenu + campaign/daily progress detail), the campaign sector briefing (drawBriefing — typewriter dialogue, threat readout; sets game.briefRevealed), story comm cards (drawComms), pause, game-over/victory (drawGameOver + the campaign epilogue), banners, the touch overlay. Pure drawing — Game owns state." },
    "src/hangar.js": { "exports": "Hangar", "role": "Upgrade shop in two guises: between runs ('hangar' state, LAUNCH starts a run) and DOCKED mid-campaign ('dock' state via open({docked:true}) — UES Orion header, banked-credits readout, DEPART resumes the run, Esc departs too, plus the flight-deck vignette: dock dialogue, ship under repair, sparks/scanline). Keyboard + mouse/touch. Fires Ach.onUpgrade() on a successful buy." },
    "src/settings.js": { "exports": "SettingsMenu", "role": "The 'settings' state. Volume sliders + shake/reduced-motion toggles + difficulty/touch-controls cycles. Keyboard (arrows/Enter) and click/drag (rebuilds hit-rects each draw). Persists via Meta.setSetting (-> save + applySettings)." },
    "src/game.js": { "exports": "Game", "role": "ORCHESTRATOR. State machine (incl. 'briefing'), modes (newGame(mode), dailyMods), campaign flow (enterSector/launchSector/startCampaignWave, sector/sectorWave, warpTimer loot window, say() comm queue, sector bonus + victory on def.final), all pools, collisions, scoring/combos, wave+boss progression + mini-boss escorts, hazards (per-sector hazardMul/bigAsteroid in campaign), drones, magnet, screen shake, banners, achievement hooks, and meta/credits." },
    "src/main.js": { "exports": "Assets (global), loadAssets(), boot(), the rAF loop", "role": "Asset preloader + loading screen, responsive canvas sizing, fixed-timestep loop." }
  },

  "forTheVisualsTeam": {
    "statusNote": "DELIVERED in phase 9 (2026-06-05). The AAA visual & juice pass is in: src/postfx.js (offscreen bloom), the Graphics Quality setting (CONFIG.quality + Meta.quality/fx/extras/trailsOn), Particles.Shockwave/SpriteBurst emitters, Game.freeze/punchZoom (hit-stop + zoom), and effects woven through every entity's draw(). The sprites were KEPT and elevated (the unused VFX columns of boss.png are now used by SpriteBurst). Constraints below were all honoured. This section is retained for the NEXT iteration (e.g. higher-res sheets, WebGL).",
    "mission": "Replace/augment the programmer-art rendering with AAA visuals WITHOUT changing gameplay timing or the file:// no-build constraint. Most draw code is isolated in *.draw(c) methods using a single Canvas 2D context.",
    "noBuildConstraint": "Keep classic <script> tags (no ES modules / no fetch) so index.html still runs by double-click. If you need a build pipeline or asset loading via fetch, you must add a static server and update howToRun + main.js loadAssets.",
    "whereRenderingLives": {
      "perEntity": "Each entity owns a draw(c): Player.draw, Enemy.draw, Boss.draw, Bullet.draw + Beam.draw (projectile.js), PowerUp.draw, Asteroid.draw (hazard.js), Drone.draw, Particle/Popup.draw (particles.js), Starfield.draw.",
      "uiAndScreens": "src/ui.js (HUD/menu/pause/gameover/banner/touch overlay), src/hangar.js (shop), src/settings.js (settings), src/achievements.js (toasts + viewer).",
      "compositing": "Game.draw() applies the screen-shake translate around drawWorld only; HUD/overlays/toasts draw in screen space after. Many effects use ctx.globalCompositeOperation = 'lighter' for additive glow."
    },
    "artAssets": {
      "spriteSheets": {
        "player.png": "560x120, 4x 140x120 (0 idle, 1 firing, 3 beam pose used).",
        "player_jets.png": "420x120, 3x 140x120 (bank left/center/right, drawn behind ship).",
        "beetlemorph.png": "240x320, 3 cols x 4 rows of 80x80 (cols 0/1 idle flap; 4 colour rows). Stinger pins frameY=1.",
        "rhinomorph.png": "480x320, 6 cols x 4 rows of 80x80 (cols 0-3 damage frames; 4 colour rows). Splitter pins frameY=2.",
        "boss.png": "2200x800, 11 cols x 4 rows of 200x200 (frameX 0/1 idle; frameY = archetype/tier colour). Boss archetypes use frameY 0/1/2; mini-bosses use frameY 3."
      },
      "backgrounds": "10 space backdrops in assets/ (background*.png + backgroundEarth.png). Endless cycles them per sector via Starfield.setBackground(level-1); the campaign pins one per sector (`bg` in CAMPAIGN_SECTORS — backgroundEarth, index 8, is sector 5).",
      "palette": "CONFIG.colors is the single source of truth for entity/UI hues. Bosses get distinct bullet colours via their def.color (Overlord=boss orange, Weaver=rhino purple, Warden=danger red).",
      "fonts": "Orbitron (Google Fonts, non-render-blocking) with a system fallback. All text goes through Utils.text (drop-shadow + optional glow)."
    },
    "suggestedVisualUpgrades": [
      "Higher-res sprite sheets / animated frames for player, enemies, bosses (keep the frame layout in CONFIG.sprites, or extend it).",
      "Replace procedural Asteroid drawing (hazard.js) with sprites if desired.",
      "Richer particle textures / additive bloom; reduced-motion already thins counts (respect Meta.reducedMotion()).",
      "Boss attack telegraphs / unique muzzle FX per attack 'type' in Boss.runStep.",
      "Parallax depth, nebula layers, and a proper finale set-piece over Earth on Campaign victory.",
      "Polished mode chips / buttons / toasts; the layout + hit-rects already exist in ui.js + achievements.js.",
      "On-screen touch buttons (UI.drawTouchControls) are functional placeholders — restyle freely; geometry is CONFIG.touch and must stay in sync with Input.buttonRect."
    ],
    "doNotBreak": [
      "Coordinate conventions (top-left AABB) and the center-based spawnBullet API.",
      "Reduced-motion + screen-shake gating (Meta.reducedMotion()/shakeEnabled()) — accessibility.",
      "Deterministic Daily: don't route Wave composition through unseeded randomness (it uses wave.rng / game.waveRng()).",
      "The fixed-timestep contract: render may change freely, but update(dt) timing must not."
    ]
  },

  "forTheOptimizationTeam": {
    "mission": "Drive the game to a locked frame rate (target 60fps; degrade gracefully on weak hardware) across every state and graphics tier WITHOUT changing gameplay timing or the no-build file:// constraint. Audit pooling, eliminate per-frame allocations (GC stutter), and cut the most expensive Canvas-2D operations. The phase-9 visual/juice pass deliberately leaned on shadowBlur + per-frame gradients for the look — that is where the cost now lives, and it is concentrated in the *.draw(c) methods.",
    "measurementFirst": "There is NO checked-in FPS number. The visual team's automated frame-timer (headless Edge via the DevTools Protocol) hung whenever the shell auto-backgrounded it — Edge's remote-debugging server does not initialise when the process is spawned detached; the FOREGROUND screenshot harnesses worked fine. So: profile in a REAL browser. Use the DevTools Performance panel (record 5-10s of a busy boss wave + asteroids at High), or run a headless CDP timer in the foreground only. Frame budget = 16.67ms at 60fps. Always measure per-tier (Low/Med/High via Settings -> GRAPHICS) AND on a mid/low-end device, not just a fast desktop. Watch the Performance panel's GC bands — minor-GC spikes lining up with explosions confirm the allocation hotspots below.",
    "topCostsByImpact": [
      "shadowBlur — THE big one. 32 call-sites across 14 files; the worst is Particle.draw (particles.js) which sets shadowColor/shadowBlur PER glowing particle, up to ~1200 live during explosions. shadowBlur is among the most expensive Canvas-2D operations. WIN: pre-render each glow ONCE to a small offscreen canvas (a radial 'blob' sprite) and drawImage it additively (globalCompositeOperation='lighter') — zero runtime blur. A tiny atlas keyed by colour bucket + size covers particles, bullet glows and muzzle flashes.",
      "Per-frame gradient allocation in hot draws — createRadialGradient/createLinearGradient are re-allocated EVERY frame PER entity. Worst offenders: enemy 'orb' bullets (projectile.js Bullet.draw, ~L53, up to 160 live), power-up halos (powerup.js ~L62, up to 24), asteroid bodies (hazard.js ~L98). WIN: cache or pre-render to sprites; a single pre-rendered orb sprite per bullet colour also removes the orb's shadowBlur. (Beam's 3 gradients and the HUD/boss-bar gradients are once-per-frame singletons — low priority.)",
      "Per-frame array allocation (GC churn) — Game.updateHazards rebuilds this.asteroids via .filter EVERY frame (game.js ~L350); Wave.update rebuilds enemies via .filter plus extra .filter scans for diverCount/formation (wave.js ~L57/L80/L93); achievements toasts (achievements.js ~L64) and starfield shooting-stars (starfield.js ~L67) filter each frame too. WIN: compact in place (swap-and-pop), iterate without allocating, and cache derived counts.",
      "Trail-point allocation — Bullet.trail (projectile.js ~L22), PowerUp.hist (powerup.js ~L37) and Drone.hist (drone.js ~L34) call .unshift({x,y}) every frame per entity: unshift is O(n) AND allocates a fresh object each frame. WIN: ring buffers of pre-allocated points indexed by a moving head.",
      "Linear-scan pool acquisition — spawnBullet/spawnEnemyBullet/spawnPowerUp use Array.find(free) (game.js ~L282/L317/L325) and Particles._get() scans up to 1200 for a dead slot on every emit (particles.js); a big explosion makes that O(n) per particle. WIN: maintain a free-list / index stack for O(1) acquire + release."
    ],
    "poolingStatus": {
      "pooled": "Shared Bullet pool: bullets(72) + enemyBullets(160). powerups(24). particles(1200) + shockwaves(24) + bursts(24) + popups(40). All correct as pools, but acquired by LINEAR SCAN (.find / for-loop), not O(1).",
      "notPooledAllocatesAtRuntime": "Asteroid (Game.spawn ~game.js L346 + Asteroid.shatter split ~hazard.js L74), Splitter minions (new Beetlemorph ~game.js L268), Wave enemies (new per wave ~wave.js L50), Drone (new per run ~game.js L115). Plus all the trail-point objects, per-frame gradients and per-frame arrays above. These are the source of the GC sawtooth during heavy waves and boss deaths.",
      "recommendation": "Add real free-list pools for Asteroid and the enemy/minion classes (or recycle Wave.enemies between waves); convert every '.find(free)' acquisition to an index-based free-list. Enemies are short-lived and spiky (minions on Splitter death) — prime pooling candidates."
    },
    "modernTechniques": [
      "Pre-rendered sprite atlas for glows/orbs/particles via an offscreen canvas + drawImage — removes runtime shadowBlur AND gradients (the single biggest win; do this first).",
      "OffscreenCanvas for the bloom buffers (postfx.js already uses plain offscreen canvases; OffscreenCanvas is a drop-in where supported and keeps the no-build constraint). A dedicated render Worker is possible but optional/advanced.",
      "Batch additive draws: group everything using globalCompositeOperation='lighter' so the composite mode + shadow state are set once per group, minimising state thrash.",
      "Aggressive culling: bullets already free themselves off-screen; extend the same early-out to skip draw() for fully off-screen or zero-alpha entities.",
      "Optional: split the mostly-static HUD onto its own layer/canvas from the moving world (dirty-rect style) so the HUD isn't recomposited every frame.",
      "Reuse & extend the Graphics Quality tiers (CONFIG.quality + Meta.quality/fx/extras/trailsOn) — they already gate the heavy FX. Wire any new perf budget (e.g. a 'potato' tier with bloom off + tighter particle caps) through them so Low stays cheap by construction."
    ],
    "postfxNote": "src/postfx.js is already allocation-free per frame: it reuses two small bloom buffers (resized only on a quality change) and uses drawImage + ctx.filter, never getImageData. Its cost is the blur passes + one full-frame composite — cheap, but profile it on weak GPUs and consider lowering bloomScale/blurPasses (or a new lowest tier) if it shows up.",
    "doNotBreak": [
      "Fixed-timestep contract: update(dt) runs at CONFIG.STEP_MS and must stay deterministic — optimise RENDER freely, never sim timing. The hit-stop (Game.freeze) is a real-time sim-skip counter; keep it real-time, not a dt hack.",
      "No-build / file://: classic <script> tags only, no ES modules / fetch(). OffscreenCanvas + classic Workers are allowed (no bundler), but a Worker needs its own classic script file — verify double-click still runs.",
      "PostFX must stay getImageData-free (file:// canvas taint) and keep its feature-detect no-op fallback (covers old browsers + the Node-VM smoke harness).",
      "Accessibility gating (Meta.reducedMotion()/shakeEnabled()) and Daily determinism (seeded wave.rng / game.waveRng) must survive any refactor.",
      "Coordinate conventions (top-left AABB) and the center-based spawnBullet/spawnEnemyBullet API."
    ],
    "howToVerify": "1) node --check src/*.js. 2) Reuse the Node-VM smoke harness (drives every state/mode/tier + a reduced-motion pass; mocks the browser, so it proves no logic regressions but NOT pixels or FPS). 3) Profile FPS in a real browser before AND after each change, on the SAME busy scene (boss wave + ~6 asteroids + a smart-bomb) at each Graphics tier; confirm frame time dropped and GC bands shrank. 4) Re-capture before/after screenshots (foreground headless CDP) to confirm the look is unchanged.",
    "verificationHarnesses": "Recreated (gitignored, under .smoke/): smoke.js (Node-VM smoke harness — `node .smoke/smoke.js`), shot.js (foreground headless-Edge CDP screenshot — `node .smoke/shot.js <out.png> [low|medium|high]`), and fps.js (foreground headless-Edge render-cost profiler — `node .smoke/fps.js [low,medium,high] [rootDirOverride]`; GPU on by default, KIS_SOFTWARE=1 forces software; rootDirOverride lets you benchmark another checkout/worktree for before/after). All honour the handoff gotchas (space-in-path file URL, child-not-detached Edge launch, setTimeout stubbed so the audio loop can't hang the process).",
    "progressLog": [
      "DONE (phase 10a): shadowBlur removed from the hottest paths via src/glowatlas.js — Particle.draw (~1200/frame), enemy-orb Bullet.draw (~160/frame, also kills its per-frame createRadialGradient), and friendly-bolt Bullet.draw (~72/frame). Verified: node --check, smoke harness green, and before/after CDP screenshots at High + Low look unchanged. Fallback to shadowBlur preserved when offscreen canvases are unavailable.",
      "DONE (phase 10b): O(1) pool acquisition + zero-alloc trails. particles.js now uses per-pool free-stacks (poolFree/popupFree/shockFree/burstFree) reclaimed in _age() — no linear scan even at ~1200 live (invariant: an index is in *Free iff that slot is dead). game.js bullets/enemyBullets/powerups acquire via a rotating cursor (_acquireSlot) instead of .find(free) (cursor chosen because bullets self-free at scattered sites). New Utils Trail ring buffer replaces Bullet.trail / PowerUp.hist / Drone.hist unshift({x,y}) (was O(n) shift + a fresh object/frame/entity). Verified: node --check, smoke harness green, High screenshot unchanged.",
      "MEASURED (phase 10d, task #5): before/after render-cost profile via .smoke/fps.js (foreground headless-Edge, GPU on, synchronous draw of a heavy STATIC scene — boss + ~150 enemy bullets + 60 player bullets + 8 asteroids + power-ups + 372/612/792 particles at low/med/high; 360 frames/tier). PER-FRAME DRAW COST, before (f599630) -> after (phase 10): LOW (bloom off) avg 9.90->7.67ms (~22% faster), p99 16.6->12.3; MEDIUM median 480->15.6ms (~31x), avg 531->31ms; HIGH median 687->16.0ms (~43x), avg 733->31.7ms. The bloom tiers collapsed because pre-pass each of ~600-800 glowing particles paid a ctx.shadowBlur — pre-rendering glows removed that. Caveats: the synchronous (no-vsync) loop induces GPU-flush stalls, so the bloom-tier p95/max (e.g. after-HIGH max 403ms) are bench artifacts, not real per-frame cost; medians are the reliable signal. Headless --disable-gpu makes ctx.filter blur software-slow (~190ms) — always run the profiler GPU-on (the default; set KIS_SOFTWARE=1 to force software). This is an EXTREME static scene; normal gameplay is far lighter.",
      "DONE (phase 10c): killed per-frame array allocation + remaining hot gradients. New Utils.compact(arr,keep) compacts in place (order-preserving, zero alloc) and replaces the per-frame .filter at game.updateHazards (asteroids), Wave.update (enemies cull), Starfield (shooting stars) and Ach.update (toasts). Wave.update now does ONE pass over enemies into a reused this._formation scratch + a diver counter (was 3 filters/frame; the diverCount getter is kept but no longer called in the hot path). powerup.js halo now blits a pre-rendered GlowSprites.blob scaled by the pulse (was createRadialGradient/frame); hazard.js bakes each asteroid's static body (silhouette+gradient+craters+rim) to a per-rock offscreen sprite ONCE (_buildSprite/_drawBody) so per-frame draw is one rotated drawImage; Shockwave dropped its shadowBlur (only spawns at fx tiers, which bloom). Verified: node --check, smoke harness green, High screenshot (asteroids+powerups+shockwave) unchanged.",
      "SHIPPED (phase 10e, task #6): deployed to GitHub Pages (free, served from main/root) — LIVE at https://knotenvy.github.io/KnotzInvadeSpace/ . The 6 perf+docs commits were fast-forwarded onto main and pushed; Pages was already enabled and auto-rebuilt. No code changes were needed for https (relative paths, no fetch/ES modules; bloom works even better than file://). Verified end-to-end: the live URL boots and renders the full menu in headless Edge (a fully-rendered menu proves every script loaded over https). README updated with the play link. NOTE: pushing used git's cached credential manager; `gh` CLI was NOT logged in (so Pages couldn't be toggled via gh api — it was already on).",
      "DONE (phase 10f): object pools + off-screen culling. Enemy + subclasses and Asteroid now have reset() (constructors delegate to it) so instances recycle: Game.enemyPools (Map keyed by constructor) via acquireEnemy()/releaseEnemy() — wave.create()/_flushMinions acquire, the wave cull releases dead ones; Game.asteroidPool via spawnAsteroid() — updateHazards/Asteroid.shatter acquire, the cull (Utils.compactRelease) releases. Asteroid re-bakes its body sprite on reset but REUSES the canvas when the size is unchanged. Off-screen culling: Utils.offscreen() early-outs Enemy.draw (whole formation during warp-in entry, divers below) and Asteroid.draw (entry from above), with margins > max screen-shake so nothing pops. Verified: node --check, smoke harness (now asserts enemy+asteroid pool release+reuse) green, and screenshots of a settled 8-enemy formation + the boss/asteroid scene render correctly. NOTE: this trims GC spikes at wave-start/Splitter-death (not visible in the static-scene draw bench).",
      "OPTIONAL remaining (nice-to-have, low priority): split the static HUD onto its own canvas layer (dirty-rect); a 'potato' quality tier (bloom off + tighter particle caps); light per-particle off-screen culling."
    ]
  },

  "keySystems": {
    "metaProgression": {
      "credits": "Earned at run end: floor((score*0.015 + waves*8 + bosses*75) * difficulty.creditMul * dailyMul) (CONFIG.credits + Meta.award).",
      "upgrades": "7 upgrades in UPGRADES (meta.js): hull, engine, beam, cannon (twin at Lv2), magnet, drone, aegis. Applied at Player.reset()/Game.newGame() via Meta derived getters.",
      "persistence": "localStorage key 'knotz_invade_space_save_v1'. Schema v2: { version, hi, credits, upgrades, muted, settings, achievements, daily }. Meta.load() forward-migrates older saves (missing fields filled from CONFIG defaults) — never wipes credits/upgrades."
    },
    "gameModes": "CONFIG.modes. CAMPAIGN = the scripted story (see keySystems.campaign). ENDLESS = unbounded, formula-driven waves + 4-boss rotation. DAILY = date-seeded run with 2 random CONFIG.daily.modifiers; deterministic formations (Utils.makeRng + Daily.waveSeed); per-day best in Meta (shown on the menu).",
    "campaign": "OPERATION HOMECOMING (src/campaign.js = ALL the content; CONFIG.campaign = the numeric knobs: sectorBonus, waveDelay, bossDelay, warpWindow, warpRamp). Flow: newGame('campaign') -> enterSector(1) (clears battlefield, sets backdrop, state='briefing', starfield warps) -> drawBriefing (sector map + typewriter; FIRE fast-forwards via game.briefRevealed, then launches with a screenFlash) -> startCampaignWave per beat with nextWaveTimer breathers between beats: scripted Wave (exact cols/rows/speed/mix) or, past the last wave, the sector's named boss (Boss opts.def; tier = sector) — the pre-boss beat is longer and plays def.taunt. wd.mini spawns a scripted escort; wd.say/clearSay/dock queue comm/dock lines. Boss death: Meta.setCampaignBest(sector); if def.final -> victory (+ Meta.addCampaignWin, 5-line epilogue); else sector bonus -> warpWindow loot window (starfield ramps to warp over the last warpRamp ms, no hazard spawns) -> enterDock(n+1): banks credits mid-run (bankCampaignCredits — endGame settles only the remainder), Player.refit() field repairs, Hangar in docked guise; DEPART/Esc -> departDock (refit again + rebuild drones) -> enterSector(n+1). Sector hazard/aggro come from the sector def (hazardMul 0 = clear skies in sector 1). HUD: sector readout + wave pips.",
    "difficulty": "CONFIG.difficulty[easy|normal|hard] multipliers read via Meta.diffMods(): enemyFireMul, enemySpeedMul, bossHpMul, bossFireMul, creditMul, lifeBonus. Chosen in Settings.",
    "bosses": "Data-driven (boss.js). 3 archetypes rotate by tier on boss waves; mini-bosses escort some normal waves. Attack 'steps' compose patterns; phases driven by remaining HP.",
    "achievements": "14 in ACHIEVEMENTS (achievements.js). Ach.* hooks fire from game.js/hangar.js; toasts slide in (Ach.drawToasts) in any state; viewer is the 'achievements' state.",
    "settings": "settings.js / 'settings' state. Volume (master/music/sfx) -> Sound.setVolumes; screen shake + reduced motion (accessibility); difficulty; touch controls. All persist instantly.",
    "input": "input.js multi-touch. On-screen buttons live when Input.buttonsActive() (setting auto/on/off + device touch). CONFIG.touch is the shared layout for Input.buttonRect + UI.drawTouchControls.",
    "deferredMinionSpawn": "Splitter death enqueues minions via game.queueMinion(); flushed in Game._flushMinions() AFTER collision resolution. Follow this for any mid-frame entity spawn.",
    "screenShake": "game.shake(mag, durMs) — no-op when Meta.shakeEnabled() is false. Applied as a translate around drawWorld only.",
    "audio": "Sound singleton. Named SFX + startMusic(tempo)/stopMusic(). Volumes via setVolumes()/_applyVolumes()."
  },

  "controls": {
    "move": "Arrow Left/Right or A/D, on-screen ◀ ▶, or (when touch buttons are off) drag",
    "fire": "Space, on-screen FIRE, or (buttons off) tap & hold",
    "beam": "Shift or X, or on-screen BEAM",
    "pause": "Esc or P",
    "mute": "M",
    "menu": "Left/Right select mode · Enter/Tap or click LAUNCH to start · H Hangar · O Settings · A Awards (all buttons are clickable)",
    "briefing": "FIRE / Enter / Tap — fast-forward the dialogue, then ENGAGE (launch) · Q / Esc — abort to menu",
    "dock": "↑ ↓ select · Enter / click — buy, or DEPART on the bottom button · Esc — depart (mid-run: no quit-to-menu)",
    "settings": "Arrows select, Left/Right adjust, Enter toggle, click/drag sliders, Q/Esc back",
    "achievementsViewer": "Enter / Tap / Q to close",
    "gameover": "Enter/Tap retry · H hangar · O settings · A awards · Q menu",
    "quitToMenu": "Q (when paused or on game-over)"
  },

  "verification": {
    "philosophy": "No automated suite is checked in (scratch harnesses kept out of the repo). Two reproducible techniques were used heavily this build:",
    "smokeTest": "Node VM harness: mocks the browser (Proxy 2D ctx, fake Image/localStorage/window, stub Assets), concatenates all src/*.js EXCEPT main.js into one VM context, then drives game.update/draw across every state AND mode. Validates (cumulative): all bosses + mini-bosses to death, daily determinism, achievement rollups, menu clicks, save migration, enemy/asteroid pool recycling, AND (phase 11) the campaign: briefing state on newGame, launchSector, scripted boss forcing, the post-boss warp -> sector-2 briefing, and a FULL scripted playthrough to victory asserting Meta campaign {best,wins} persistence.",
    "visualScreenshots": "Headless Edge via the DevTools Protocol from Node (global fetch + WebSocket). Launch msedge --headless=new --remote-debugging-port=PORT --remote-allow-origins=* --user-data-dir=<unique>, poll /json/list for the page target, Runtime.evaluate to force a state (e.g. window.game.openAchievements / newGame('daily') / spawn a Boss), then Page.captureScreenshot. window.game is the live instance.",
    "gotchas": [
      "Edge --screenshot=PATH silently fails if PATH has a space; capture via CDP or write to C:/Temp/...",
      "Give each concurrent Edge launch a unique --user-data-dir + port and kill lingering msedge.exe between runs.",
      "In the VM harness, expose `canvas` on the sandbox and build the Assets stub AFTER CONFIG exists; loop conditions that re-read game.boss can race with a boss dying inside update() — re-check before dereferencing."
    ]
  },

  "knownIssuesAndGotchas": [
    "Single active wave at a time (game.waves holds 0 or 1). A mini-boss shares the single game.boss slot and blocks sector-clear until killed.",
    "ES modules intentionally avoided. Introducing import/export or fetch() breaks the file:// double-click workflow (serve over http instead).",
    "Daily determinism covers WAVE COMPOSITION + modifiers; power-up drop types and particles still use Math.random (cosmetic / fair-enough). If you want fully deterministic dailies, route those through game.waveRng-style seeded streams too.",
    "Orbitron font loads non-render-blocking; layout assumes the system fallback is acceptable offline.",
    "Mouse does not actuate the on-screen touch buttons unless touchControls='on' (they're driven by the touches map; mouse is added to it on press). 'auto' hides them on non-touch devices.",
    "Balance is improved but still first-pass beyond difficulty modes; tune CONFIG.credits / UPGRADES / CONFIG.wave / CONFIG.difficulty against real playtests.",
    "Achievement toasts draw in every state and can briefly overlap a busy screen if many unlock at once."
  ],

  "roadmap": {
    "primaryNext": "FEATURE-COMPLETE, OPTIMIZED, SHIPPED, and now with a full STORY CAMPAIGN (phase 11; push main to deploy it live). No required work remains. Optional next steps: campaign balance playtests (sector 4-5 difficulty curve is authored but unvalidated by humans), campaign-specific achievements (e.g. per-sector clears, beat the Mothership hitless), a sector-select / checkpoint option (would need an economy decision), the leftover perf nice-to-haves (HUD layer split, 'potato' tier), or the secondarySuggestions below.",
    "completed": "AAA VISUAL OVERHAUL (phase 9) + PERFORMANCE OPTIMIZATION (phase 10) + SHIPPED to GitHub Pages + STORY CAMPAIGN OVERHAUL (phase 11 — Operation Homecoming). All delivered & verified.",
    "secondarySuggestions": [
      { "item": "Online / shared daily leaderboard", "why": "Daily best is local-only; a backend would make the seeded mode competitive.", "where": "daily.js + a service" },
      { "item": "More bosses / mini-bosses + attack 'step' types", "why": "The data-driven system makes this cheap.", "where": "boss.js BOSS_TYPES/MINIBOSS_TYPES + runStep" },
      { "item": "More achievements + cosmetic unlocks tied to credits", "why": "Extends the meta loop.", "where": "achievements.js + meta.js" },
      { "item": "Colorblind-safe palette option", "why": "Builds on the accessibility settings already in place.", "where": "Settings + CONFIG.colors" },
      { "item": "Deeper economy/difficulty playtest tuning", "why": "Numbers are reasonable but unvalidated at scale.", "where": "CONFIG.credits, UPGRADES, CONFIG.wave, CONFIG.difficulty" }
    ]
  },

  "quickStartForNewDev": [
    "1. Open index.html in a browser — it just runs (no install).",
    "2. Read README.md (player overview), then this handoff. If you're on PERFORMANCE, read forTheOptimizationTeam first (it pinpoints the hotspots); forTheVisualsTeam documents what the render layer does.",
    "3. Skim src/config.js (all knobs) and src/game.js (orchestrator); they anchor everything. For rendering, every entity's draw(c) + src/ui.js are your map.",
    "4. window.game in the console drives the live game (openSettings/openAchievements/newGame('daily')/spawn a Boss).",
    "5. Sanity-check edits with node --check src/*.js; for deeper checks reuse the Node-VM smoke technique under 'verification'.",
    "6. Keep the no-build (classic <script>, file://) and fixed-timestep contracts intact."
  ]
}
